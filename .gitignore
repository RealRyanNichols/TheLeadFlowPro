node_modules
.next
out
dist
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.turbo
prisma/dev.db
prisma/dev.db-journal
coverage
tsconfig.tsbuildinfo
*.tsbuildinfo
.art-review/
supabase/.temp/
test-results/
.env*
# The factory template's env example carries variable NAMES only (a test
# enforces that); the scaffolder copies it into every client site.
!factory/template/.env.example

# Factory output (client sites scaffolded by scripts/new-client.ts)
build/

# local scratch
query_leads.cjs
