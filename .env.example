# --- Database (Postgres). Free tier: https://neon.tech ---
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# --- NextAuth ---
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
LEADFLOW_ADMIN_EMAILS="ryan@realryannichols.com,hello@theleadflowpro.com"
NEXT_PUBLIC_SITE_URL="https://www.theleadflowpro.com"

# --- Email notifications ---
RESEND_API_KEY=""
RESEND_FROM_ADDRESS="Ryan @ The LeadFlow Pro <hello@theleadflowpro.com>"
LEADFLOW_NOTIFY_EMAIL="Hello@TheLeadFlowPro.com"

# --- Anthropic (Claude AI) ---
ANTHROPIC_API_KEY="sk-ant-..."

# --- OAuth providers (add when ready) ---
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# --- Stripe checkout (server-side unless explicitly publishable) ---
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""

# --- LeadFlow buyer integrations ---
# Server-side only. Enables encrypted storage for webhook secret headers and future provider tokens.
LEADFLOW_INTEGRATION_SECRET_KEY=""

# --- Social platform OAuth ---
INSTAGRAM_CLIENT_ID=""
INSTAGRAM_CLIENT_SECRET=""
TIKTOK_CLIENT_KEY=""
TIKTOK_CLIENT_SECRET=""
TWITTER_CLIENT_ID=""
TWITTER_CLIENT_SECRET=""
FACEBOOK_APP_ID=""
FACEBOOK_APP_SECRET=""
# Canonical server-only names used by the LeadFlow Facebook publisher.
# Admins can also save the App ID and App Secret into encrypted Supabase Vault.
META_APP_ID=""
META_APP_SECRET=""
META_OAUTH_REDIRECT_URI="https://www.theleadflowpro.com/api/admin/social/meta/callback"
META_PAGE_ID="887023637835514"
META_PAGE_ACCESS_TOKEN=""
YOUTUBE_CLIENT_ID=""
YOUTUBE_CLIENT_SECRET=""

# --- Live public/social stats ---
YOUTUBE_API_KEY=""
X_BEARER_TOKEN=""
NEXT_PUBLIC_META_PIXEL_ID="1012793881211964"
META_GRAPH_VERSION="v26.0"
# Page/professional asset token. Keep this server-side only.
FB_PAGE_ID=""
FB_PAGE_ACCESS_TOKEN=""
# Optional alias used by the Meta diagnostics endpoint.
META_ACCESS_TOKEN=""
IG_ACCESS_TOKEN=""

# --- Google Search Console (analytics dashboards) ---
# Create a Google Cloud service account, enable the "Google Search Console API",
# add the service account email as a user on the Search Console property, then:
GSC_CLIENT_EMAIL=""
# The private_key field from the service-account JSON (\n-escaped is fine).
GSC_PRIVATE_KEY=""
# The property exactly as GSC knows it: "sc-domain:theleadflowpro.com" for a
# domain property, or "https://www.theleadflowpro.com/" for a URL-prefix one.
GSC_SITE_URL=""

# --- Lead Intelligence / Organic Radar ---
# Optional. PageSpeed can run without a key at low volume, but a key gives better quota.
GOOGLE_PAGESPEED_API_KEY=""
# Optional. Enables /admin/radar to seed local prospects from Google Places API.
GOOGLE_PLACES_API_KEY=""

# --- Twilio (Missed-Call Auto Text-Back + inbound SMS) ---
# Create a sub-account, buy a number, enable SMS on it, then set the
# Voice + Messaging webhooks to the URLs shown on
# /dashboard/leads/missed-call (per-user).
# If these are empty, sendSms() logs the payload and returns { skipped: true }
# so development + preview deploys don't explode.
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""

# --- LeadRep Orchestrator (server-side only; never use NEXT_PUBLIC_) ---
XAI_API_KEY=""
SUPABASE_URL="https://hpzpwfymwfgwspaixrxi.supabase.co"
NEXT_PUBLIC_SUPABASE_URL="https://hpzpwfymwfgwspaixrxi.supabase.co"
SUPABASE_ANON_KEY=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
LEADREP_BUS_URL="https://hpzpwfymwfgwspaixrxi.supabase.co"
LEADREP_BUS_SERVICE_KEY=""
LEADREP_GROK_MODE="dry_run"
LEADREP_APPROVAL_MODE="manual"
LEADREP_API_SECRET=""
GROK_MAX_DAILY_COST_USD="10"
LEADREP_GITHUB_REPOSITORY="RealRyanNichols/TheLeadFlowPro"
LEADREP_VERCEL_PROJECT="realryannichols/the-lead-flow-pro"

# Public env rule:
# Only use NEXT_PUBLIC_ for browser-safe values such as site URL, publishable keys, public analytics IDs, and Supabase anon URL/key.
# Never expose service role keys, webhook secrets, private tokens, or buyer/lead data through NEXT_PUBLIC_ variables.
