# Handoff, September 17, 2026

For Ryan. What is live, what changed, what was tested, what needs a decision.

## What is live now

PR #54 merged to `main` at Ryan's request and deployed through Vercel.

- **Tonight's swap is automatic.** At 8:30 PM Central the homepage banner,
  hero card, "03 / DO" card, the next-step chooser, `/events`, and the
  workshop welcome email stop selling seats and switch to "Get notified
  about the next workshop" plus the free ChatGPT lesson. The homepage
  re-renders within 15 minutes of that. To force it earlier, set
  `status: "past"` in `lib/site/events.ts` and deploy. `/events` stops
  forwarding to the workshop subdomain at the same moment.
- **One home for every fact.** Prices, phone, emails, legal name, links,
  the featured event, approved claims, navigation, and footer live in
  `lib/site/`. The build fails if any of them is typed by hand elsewhere.
- **Plugin.** `/plugin` rebuilt; `/plugin/docs` added (install for ChatGPT,
  Claude, Claude Code, Cursor; first tasks; data; billing; cancellation;
  changelog). Checkout still waits on the Stripe env vars.
- **Agency lane.** `/agency`, six service pages, and `/agency/start`. The
  header and homepage chooser gain "Run it for me". Prices show "confirmed
  on the scoping call" until you set them.
- **Structured data** for the organization and local business comes from
  config on the homepage and the agency pages.

## What changed, in business language

- The September 17 date, time, price, and seat count were typed in six
  places. Now they come from one entry, and the database event row wins
  when it exists.
- Every price on the site, in checkout, in the Stripe webhook, and in the
  emails reads the same number. Changing a price is one edit.
- The legacy footer that still said "Real Ryan Nichols LLC" is gone.
- Company OS links to one address everywhere.
- The Premier proof numbers carry their September 1 date and a review date.
- New lead doors: the workshop list (interest "Training and workshops") and
  the agency intake (interest "Agency: run it for me"). Both land in the CRM,
  alert hello@ and pat@, and send the applicant a note from Ryan.

## Drafted, not sent or activated

- Post-workshop attendee emails (same night, day 2, day 7):
  `lib/workshopFollowUp.ts`.
- Plugin onboarding and monthly note: `lib/pluginOnboarding.ts`.
- Three articles and the plan: `docs/articles/`.
- RealRyanNichols cross-links: `docs/realryannichols-crosslink-spec.md`.

## What was tested

- 1162 unit tests pass, including the new event state machine, config
  integrity, follow-up drafts, plugin product, agency, link, and facts-gate
  tests.
- `npm run build` (all validators plus the Next build) is green locally
  and on Vercel.
- A mobile and desktop e2e spec covers the four buyer paths
  (`tests/e2e/site-lanes.spec.ts`); run it with `npm run test:e2e`.
- Not tested: a real form submission against production (no test lead was
  created), the workshop subdomain's behaviour after tonight, and the Vercel
  Web Analytics setting (the session could not see that Vercel project).

## Decisions

See `docs/decisions-needed.md`. The three that matter tonight: leave the
swap automatic or force it, who updates the workshop subdomain, and whether
to approve the attendee follow-up (worksheet link and the day-2 offer).

## Safest next step

Nothing. The swap runs itself. Tomorrow: pick the agency prices, decide the
SMS policy (item 17), and approve or edit the follow-up emails.
